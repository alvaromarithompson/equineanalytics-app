Future<void> saveTextFile(String filename, String content) async {
  throw UnsupportedError(
    'File download not yet implemented for this platform. '
    'Use the web (Chrome) version to download CSV files.',
  );
}
